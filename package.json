{
  "name": "ts-test",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "devDependencies": {
    "tsx": "^3.12.7",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "ci:install": "yarn install --frozen-lockfile",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  }
}
